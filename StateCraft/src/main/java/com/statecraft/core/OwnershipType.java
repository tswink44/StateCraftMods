package com.statecraft.core;

/**
 * Defines who owns a chunk
 */
public enum OwnershipType {
    HIERARCHY,  // Owned by the city/state/nation (public land)
    PLAYER      // Owned by an individual player (private land)
}

